import { Page, PageHeader } from "@/components/app/page-header";

/** Placeholder until Phase 5 wires dashboard_stats(). */
export async function Dashboard() {
  return (
    <Page>
      <PageHeader title="Dashboard" description="Coming in the next phase." />
    </Page>
  );
}

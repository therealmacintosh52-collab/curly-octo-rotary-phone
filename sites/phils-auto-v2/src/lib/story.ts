/** Shared, mutable hero story state: scroll progress 0–1 and normalised mouse.
    Written by scripts/hero-story.ts, read every frame by the WebGL scene. */
export const story = { p: 0, mx: 0, my: 0 };
